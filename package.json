{
  "name": "iingapp",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:web": "pnpm --filter @iingapp/web dev",
    "dev:api": "pnpm --filter @iingapp/api dev",
    "lint": "pnpm -r lint",
    "supabase": "supabase"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "supabase": "^2.98.2",
    "typescript": "^5.4.0"
  }
}
