node_modules
dist
.wrangler
.dev.vars
.env
.env.local
*.log
.DS_Store
.vscode
coverage

# TypeScript build artifacts
*.tsbuildinfo

# Windows / shell artifacts
*.stackdump

# Large audio assets (handled via R2 storage, not git)
Contents/
.claude/settings.local.json
